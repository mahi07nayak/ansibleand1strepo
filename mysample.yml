---
- hosts: all
  become: yes
  tasks:
  - name: i want install webserver
    apt:
      name: apache2
      state: present
    when: ansible_os_family =="debian"  




  - name: i want start webserver
    service:
      name: apache2
      state: started
    when: ansible_os_family =="debian"  