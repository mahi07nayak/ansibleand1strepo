---
- hosts: mywebserver
  become: yes
  tasks:
   - name: execute linux commands
     command: hostname -f
     register: mydebugresult
  
   - name: i want to display my tasks
     debug:
       msg: "{{mydebugresult}}"